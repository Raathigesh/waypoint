import { BlockUI } from "common/Block";
import Search from "./view/Search";

const findMySymbol = {
  view: {
    path: "search",
    Component: Search
  }
} as BlockUI;

export default findMySymbol;
